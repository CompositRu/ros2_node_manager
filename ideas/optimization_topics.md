Хороший вопрос! Топики — это сложнее, чем ноды. Давай разберём.

## Проблема с топиками

```
Типичная система:
├── ~200 топиков
├── Некоторые публикуют 100 Hz (IMU, LiDAR)
├── Некоторые публикуют 1 Hz (состояние)
└── Некоторые — огромные данные (PointCloud: 1-10 MB/сообщение)
```

### Команды и их стоимость

| Команда | Время | CPU | Трафик | Можно кэшировать? |
|---------|-------|-----|--------|-------------------|
| `ros2 topic list` | 1-2 сек | Низкий | Низкий | ✅ Да, TTL 30 сек |
| `ros2 topic info /topic` | 1-2 сек | Низкий | Низкий | ✅ Да, TTL 30 сек |
| `ros2 topic hz /topic` | Постоянный процесс | Средний | Низкий | ⚠️ Частично |
| `ros2 topic echo /topic` | Постоянный процесс | Высокий | **Очень высокий** | ❌ Нет |

---

## Стратегия оптимизации

### 1. Topic List & Info — кэширование

```python
# Легко оптимизировать
topics_list = cache.get_or_fetch("topics_list", ros2_topic_list, ttl=30)
topic_info = cache.get_or_fetch(f"topic_info:{name}", ros2_topic_info, ttl=30)
```

### 2. Topic Hz — три уровня

```
┌─────────────────────────────────────────────────────────────────────────┐
│  Topic Hz Strategy                                                      │
│                                                                         │
│  Level 1: Critical Topics (постоянный мониторинг)                      │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │  Конфиг: config/monitoring.yaml                                 │    │
│  │  critical_topics:                                               │    │
│  │    - /sensing/lidar/concatenated/pointcloud                     │    │
│  │    - /localization/kinematic_state                              │    │
│  │    - /control/command/control_cmd                               │    │
│  │                                                                 │    │
│  │  Background: один процесс мониторит все critical топики        │    │
│  │  Результат: в кэше всегда есть Hz для важных топиков           │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                         │
│  Level 2: On-demand Hz (по запросу пользователя)                       │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │  User кликнул на топик → запускаем hz на 5 секунд              │    │
│  │  Показываем результат, процесс завершается                      │    │
│  │  TTL кэша: 10 секунд                                           │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                         │
│  Level 3: Live Hz (пользователь открыл детали топика)                  │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │  WebSocket подключение → запускаем hz                          │    │
│  │  Стримим результаты пока подключён                              │    │
│  │  Отключился → убиваем процесс                                   │    │
│  │  Лимит: максимум 5 одновременных live hz на сервер             │    │
│  └─────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────┘
```

### 3. Topic Echo — строгие ограничения

```
┌─────────────────────────────────────────────────────────────────────────┐
│  Topic Echo Strategy                                                    │
│                                                                         │
│  ОПАСНОСТЬ: PointCloud топик = 10 MB × 10 Hz = 100 MB/sec!            │
│                                                                         │
│  Защитные меры:                                                        │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │  1. Whitelist/Blacklist топиков                                 │    │
│  │     blacklist:                                                  │    │
│  │       - /sensing/lidar/*/pointcloud      # Слишком большие     │    │
│  │       - /sensing/camera/*/image_raw      # Слишком большие     │    │
│  │                                                                 │    │
│  │  2. Лимит сообщений                                            │    │
│  │     ros2 topic echo /topic --once        # Одно сообщение      │    │
│  │     или --max-msgs 10                    # Максимум 10         │    │
│  │                                                                 │    │
│  │  3. Truncate больших сообщений                                 │    │
│  │     Если сообщение > 10KB → показываем только структуру        │    │
│  │                                                                 │    │
│  │  4. Лимит одновременных echo                                   │    │
│  │     Максимум 3 echo процесса на весь сервер                    │    │
│  │                                                                 │    │
│  │  5. Timeout                                                     │    │
│  │     Автоматически убиваем через 60 секунд                       │    │
│  └─────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Архитектура Topic Service

```python
# server/services/topic_service.py

class TopicService:
    MAX_CONCURRENT_HZ = 5
    MAX_CONCURRENT_ECHO = 3
    ECHO_TIMEOUT = 60  # секунд
    ECHO_MAX_MSGS = 10
    ECHO_MAX_SIZE = 10 * 1024  # 10 KB
    
    BLACKLISTED_PATTERNS = [
        r"/sensing/.*/pointcloud",
        r"/sensing/.*/image_raw",
        r"/perception/.*/objects",  # Большие массивы
    ]
    
    def __init__(self):
        self._hz_processes: Dict[str, asyncio.subprocess.Process] = {}
        self._echo_processes: Dict[str, asyncio.subprocess.Process] = {}
        self._hz_results: Dict[str, float] = {}  # topic -> hz
        self._critical_monitor_task = None
    
    # ─────────────────────────────────────────────────────────────────
    # Topic List & Info (кэшируемые)
    # ─────────────────────────────────────────────────────────────────
    
    async def get_topics(self) -> List[dict]:
        """Список топиков с базовой информацией."""
        return await cache.get_or_fetch(
            "topics_list",
            self._fetch_topics_list,
            ttl=30
        )
    
    async def get_topic_info(self, topic: str) -> dict:
        """Информация о топике (тип, publishers, subscribers)."""
        return await cache.get_or_fetch(
            f"topic_info:{topic}",
            lambda: self._fetch_topic_info(topic),
            ttl=30
        )
    
    # ─────────────────────────────────────────────────────────────────
    # Topic Hz
    # ─────────────────────────────────────────────────────────────────
    
    async def start_critical_monitoring(self):
        """Фоновый мониторинг важных топиков."""
        critical_topics = config.get("critical_topics", [])
        self._critical_monitor_task = asyncio.create_task(
            self._monitor_critical_topics(critical_topics)
        )
    
    async def _monitor_critical_topics(self, topics: List[str]):
        """Периодически проверяет Hz критических топиков."""
        while True:
            for topic in topics:
                try:
                    hz = await self._measure_hz_once(topic, duration=2)
                    self._hz_results[topic] = hz
                except Exception as e:
                    self._hz_results[topic] = -1  # Ошибка
            
            await asyncio.sleep(5)  # Каждые 5 секунд
    
    async def get_hz(self, topic: str) -> Optional[float]:
        """Получить Hz топика (из кэша или измерить)."""
        # Сначала проверяем кэш критических топиков
        if topic in self._hz_results:
            return self._hz_results[topic]
        
        # Иначе измеряем
        return await cache.get_or_fetch(
            f"topic_hz:{topic}",
            lambda: self._measure_hz_once(topic, duration=3),
            ttl=10
        )
    
    async def subscribe_hz(self, topic: str, callback: Callable):
        """Live подписка на Hz (WebSocket)."""
        if len(self._hz_processes) >= self.MAX_CONCURRENT_HZ:
            raise TooManyProcessesError("Max concurrent Hz monitors reached")
        
        # Запускаем процесс и стримим результаты
        ...
    
    # ─────────────────────────────────────────────────────────────────
    # Topic Echo
    # ─────────────────────────────────────────────────────────────────
    
    def is_topic_allowed_for_echo(self, topic: str) -> bool:
        """Проверка blacklist."""
        for pattern in self.BLACKLISTED_PATTERNS:
            if re.match(pattern, topic):
                return False
        return True
    
    async def echo_once(self, topic: str) -> dict:
        """Получить одно сообщение из топика."""
        if not self.is_topic_allowed_for_echo(topic):
            raise TopicBlacklistedError(f"Topic {topic} is too large for echo")
        
        if len(self._echo_processes) >= self.MAX_CONCURRENT_ECHO:
            raise TooManyProcessesError("Max concurrent echo processes reached")
        
        # ros2 topic echo /topic --once
        result = await self._exec_echo(topic, max_msgs=1, timeout=10)
        
        # Truncate если слишком большое
        if len(str(result)) > self.ECHO_MAX_SIZE:
            return {"_truncated": True, "_size": len(str(result)), "preview": str(result)[:1000]}
        
        return result
    
    async def subscribe_echo(self, topic: str, callback: Callable, max_msgs: int = 10):
        """Live подписка на сообщения (WebSocket)."""
        if not self.is_topic_allowed_for_echo(topic):
            raise TopicBlacklistedError(f"Topic {topic} is too large for echo")
        
        if len(self._echo_processes) >= self.MAX_CONCURRENT_ECHO:
            raise TooManyProcessesError("Max concurrent echo processes reached")
        
        # Запускаем с таймаутом
        # Стримим до max_msgs или ECHO_TIMEOUT
        ...
```

---

## UI для топиков

```
┌─────────────────────────────────────────────────────────────────────────┐
│  Topics Panel                                                           │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ 🔍 Filter: [____________]  [Show inactive ☐]                   │    │
│  ├─────────────────────────────────────────────────────────────────┤    │
│  │                                                                 │    │
│  │  Topic                              Type           Hz    Pubs  │    │
│  │  ─────────────────────────────────────────────────────────────  │    │
│  │  ● /sensing/lidar/top/pointcloud    PointCloud2   10.0    1   │    │
│  │  ● /localization/kinematic_state    Odometry      50.0    1   │    │
│  │  ● /control/command/control_cmd     Control       30.0    1   │    │
│  │  ○ /planning/mission_planner/route  Route          0.0    0   │    │
│  │  🚫 /sensing/camera/front/image     Image          N/A    1   │    │
│  │     ↑ (too large for monitoring)                               │    │
│  │                                                                 │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │  Topic Detail: /localization/kinematic_state                    │    │
│  │                                                                 │    │
│  │  Type: nav_msgs/msg/Odometry                                   │    │
│  │  Publishers: 1 (/localization/pose_estimator)                  │    │
│  │  Subscribers: 3                                                 │    │
│  │  Hz: 50.2 (live 🔴)                                            │    │
│  │                                                                 │    │
│  │  [Echo Message]  [Stop Hz Monitor]                             │    │
│  │                                                                 │    │
│  │  Last message:                                                  │    │
│  │  ┌─────────────────────────────────────────────────────────┐   │    │
│  │  │ header:                                                  │   │    │
│  │  │   stamp: {sec: 1706285400, nanosec: 123456789}          │   │    │
│  │  │   frame_id: "map"                                        │   │    │
│  │  │ pose:                                                    │   │    │
│  │  │   position: {x: 123.45, y: 67.89, z: 0.0}              │   │    │
│  │  │   orientation: {x: 0, y: 0, z: 0.707, w: 0.707}        │   │    │
│  │  └─────────────────────────────────────────────────────────┘   │    │
│  └─────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Конфигурация мониторинга топиков

```yaml
# config/monitoring.yaml
topics:
  # Критические топики - мониторятся постоянно
  critical:
    - /sensing/lidar/concatenated/pointcloud
    - /localization/kinematic_state
    - /control/command/control_cmd
    - /vehicle/status/velocity
  
  # Blacklist для echo - слишком большие
  echo_blacklist:
    - /sensing/.*/pointcloud
    - /sensing/.*/image_raw
    - /perception/.*/objects
    - /map/.*
  
  # Алерты по Hz
  hz_alerts:
    - topic: /sensing/lidar/concatenated/pointcloud
      min_hz: 8.0
      alert: "LiDAR frequency dropped!"
    
    - topic: /localization/kinematic_state
      min_hz: 40.0
      alert: "Localization frequency low!"

limits:
  max_concurrent_hz: 5
  max_concurrent_echo: 3
  echo_timeout_sec: 60
  echo_max_msgs: 10
  echo_max_size_kb: 10
```

---

## Сравнение нагрузки

### Без оптимизации (наивный подход)

```
User 1: смотрит Hz 5 топиков    → 5 процессов ros2 topic hz
User 2: смотрит Hz 5 топиков    → 5 процессов ros2 topic hz
User 3: echo 2 топика           → 2 процесса ros2 topic echo
                                  ────────────────────────────
                                  12 процессов постоянно
                                  + docker exec overhead
                                  + потенциально 100MB/sec трафик
```

### С оптимизацией

```
Background: 10 critical topics   → 1 процесс (периодический)
User 1: смотрит Hz 5 топиков    → 3 из кэша, 2 on-demand
User 2: смотрит Hz 5 топиков    → Все из кэша (уже измерены)
User 3: echo 2 топика           → 2 процесса (с лимитами)
                                  ────────────────────────────
                                  ~3 процесса в среднем
                                  Трафик контролируемый
```

---

## Альтернатива: Python ROS2 API (будущее)

Если вебсервер будет **внутри Docker** или с настроенным DDS bridge:

```python
# Вместо docker exec ros2 topic hz
import rclpy
from rclpy.node import Node

class TopicMonitor(Node):
    def __init__(self):
        super().__init__('topic_monitor')
        self.hz_counters = {}
    
    def monitor_hz(self, topic: str, msg_type):
        self.create_subscription(msg_type, topic, self._count_callback, 10)
    
    def _count_callback(self, msg):
        # Считаем сообщения напрямую
        ...
```

**Преимущества:**
- Нет overhead docker exec (экономия 1-2 сек на команду)
- Один процесс для всего
- Прямой доступ к данным

**Недостатки:**
- Требует запуск внутри ROS2 окружения
- Сложнее настройка

Это можно рассмотреть в Phase 4 (Scale).

---

## Итого по оптимизации топиков

| Функция | Стратегия | Процессы |
|---------|-----------|----------|
| topic list | Кэш 30 сек | 0 (background) |
| topic info | Кэш 30 сек | 0 (on-demand) |
| topic hz (critical) | Background мониторинг | 1 (постоянный) |
| topic hz (other) | On-demand + кэш | 0-5 (по запросу) |
| topic echo | On-demand + лимиты | 0-3 (по запросу) |

**Максимум одновременных процессов:** ~8-10 вместо потенциально сотен.

---

Добавить это в документацию архитектуры?